#!/bin/bash
APP="/Applications/NetSplit.app"
[[ -d "$APP" ]] || exit 0

CONSOLE_USER="$(stat -f '%Su' /dev/console 2>/dev/null || true)"
if [[ -n "$CONSOLE_USER" && "$CONSOLE_USER" != "root" && "$CONSOLE_USER" != "loginwindow" ]]; then
  su -l "$CONSOLE_USER" -c "open -a '$APP'" >/dev/null 2>&1 || true
fi
exit 0
